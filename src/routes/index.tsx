import { createFileRoute } from "@tanstack/react-router";
/** Placeholder — replaced by scripts/restore-sources.mjs at build/dev time */
export const Route = createFileRoute("/")({
  component: () => <div className="p-8">Meridian loading…</div>,
});
